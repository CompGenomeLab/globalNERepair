README

The Python_Scripts directory includes the pipelines that are used for multiple applications during this project.
These are not the all scripts that are used during this process but the ones that are not included here are for simple file transfer, file naming or output check.
