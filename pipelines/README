README

The pipeline directory includes four seqeuncing technique are uploaded. 
These pipelines are wrote such a way that they could be used either for downsampling a BED file or just applying the protocol written inside the pipelines. 

##The downsampling part need to be performed at the specified point inside the each type of sequencing method pipeline.
